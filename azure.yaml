name: azure-mcp-retrieval
metadata:
  template: azure-mcp-retrieval@0.0.1-alpha

services:
  mcp-server:
    project: ./src
    language: python
    host: containerapp
    docker:
      path: ./Dockerfile
      context: .
